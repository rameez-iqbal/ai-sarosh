
<div class="{{$columns}}" data-aos="zoom-in-up">
    <div class="libraries d-flex flex-column align-items-center gap-3">
        <img src="{{$image}}" class="w-100 h-100" alt="no-image">
        <a href="{{$href!='#' ? route($href) : '#'}}" class="main-btn">{{$name}}</a>
    </div>
</div>
            
